export class Auth {
  // @PrimaryGeneratedColumn('uuid')
  // id: string;
  // @Column()
  // jwt: string;
  // @Column()
  // tokenHash: string;
  // @ManyToOne(() => User)
  // user: User;
  // { type: 'timestamptz' }
  // @Column()
  // created_at: Date;
  // @CreateDateColumn()
  // updated_at: Date;
  // @Column({ default: false })
  // revoked: boolean;
}
